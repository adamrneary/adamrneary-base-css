{
  "name": "adamrneary-base-css",
  "version": "0.0.1",
  "description": "Base css and assets for use across projects",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Adam Neary (adamrneary)",
  "license": "MIT",
  "dependencies": {
    "bootstrap": "^3.3.0"
  }
}
